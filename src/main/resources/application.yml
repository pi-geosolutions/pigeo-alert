spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pialert
    username: pigeo
    password: pigeo123
    platform: POSTGRESQL
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect
